# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Linters / type checkers
.ruff_cache/

# Virtual environments
.venv

# world-drawer runtime data (canvas save)
# NOTE: data/world.db はアーカイブとしてローカル保持。Git管理外のまま。
data/
!data/.gitkeep

# Rust
backend/target/
backend/.env

# server-local config override (untracked, wins over config.jsonc)
config.local.jsonc
