# Rustバックエンド用 (config.jsoncより環境変数優先)
DATABASE_URL=postgresql://postgres:secret@127.0.0.1:5432/worlddrawer
# config.jsonc側: redisUrl / turnstile.secretKey / adminUIDs / corsOrigins / siteUrl
RUST_LOG=info
